# Personal Access Token do GitHub (OPCIONAL)
# Sem token: 60 requisições/hora. Com token: 5000 requisições/hora.
# Crie um em https://github.com/settings/tokens — não precisa de nenhum escopo (dados públicos).
GITHUB_TOKEN=

# Porta do servidor (padrão: 3001)
PORT=3001
